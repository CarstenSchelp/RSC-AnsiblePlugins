- name: Sample ansible remote
  hosts: localhost

  tasks:
    - name: Create ansible-playbook-directory
      file:
        path: /behnaz/{{ folder_name }}
        state: directory
        recurse: yes
