- name: Sample ansible remote
  hosts: localhost

  tasks:
    - name: Create ansible-playbook-directory
      file:
        path: /behnaz/{{ carstens_folder }}
        state: directory
        recurse: yes
